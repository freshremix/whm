services:
  whm:
    image: node:20
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install --no-audit --no-fund && node app.js"
    ports:
      - "3021:3000"
    environment:
      PORT: 3000
