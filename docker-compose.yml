services:
  whm:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && npm start"
    ports:
      - "3021:3000"
    environment:
      PORT: 3000
